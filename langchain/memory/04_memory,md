## 代码解析：`04_memory.py` - 带记忆的对话链

这是一个 **多轮对话记忆系统** 的完整实现，展示了 LangChain 的会话上下文管理能力。

ConversationBufferMemory 或 ConversationStringBufferMemory 一起使用的 LLMChain 或 ConversationChain 内存类已被废弃
使用现有现代原语处理对话历史的方法有：
使用 LangGraph 持久化 以及对消息历史记录的适当处理
使用 LCEL 与 RunnableWithMessageHistory 结合，并对消息历史记录进行适当的处理。
大多数用户会发现 LangGraph持久化 在使用和配置上比相应的LCEL更简单，尤其是在更复杂的用例中。

### 整体架构

```
用户输入 → 模板注入历史 → 模型推理 → 输出
              ↑
         历史消息存储 ←── session_id 隔离
```

### 核心组件解析

| 组件 | 作用 |
|------|------|
| `MessagesPlaceholder` | 占位符，自动填充对话历史 |
| `InMemoryChatMessageHistory` | 基于内存的会话存储 |
| `RunnableWithMessageHistory` | 包装器，自动管理历史注入 |

### 关键设计#### 1. LCEL 链式组合
```python
base_chain = prompt | model | StrOutputParser()
```
用 `|` 符将三个组件串联，实现声明式的数据流。

#### 2. 会话隔离机制
```python
session_store = {}  # 字典存储多个会话

def get_session_history(session_id: str):
    if session_id not in session_store:
        session_store[session_id] = InMemoryChatMessageHistory()
    return session_store[session_id]
```
- 不同 `session_id` 的对话历史完全隔离
- 按需创建，空闲会话不占用资源#### 3. 记忆注入
```python
chain_with_memory = RunnableWithMessageHistory(
    base_chain,
    get_session_history,
    input_messages_key="question",
    history_messages_key="chat_history"  # 与 MessagesPlaceholder 一致
)
```
- 自动在每次调用时从存储获取历史
- 注入到 prompt 的 `chat_history` 位置

### 执行流程（第 76-93 行）

```
对话1: "什么是 Docker？" 
  → 模型首次回答，无上下文依赖

对话2: "它和虚拟机有什么区别？"
  → 自动携带对话1的历史
  → 模型理解"它"指代 Docker

对话3: "给我一个最简单的 Dockerfile"
  → 携带完整上下文
  → 可生成针对性示例
```

### 技术亮点

1. **配置与逻辑分离**：`chat()` 函数只需传入参数，历史管理由框架处理
2. **可变温度**：`temperature=0.7` 在创意（0.7）和确定性（0）间取得平衡
3. **可观测性**：通过 `print_memory()` 直接检查记忆内容，便于调试

### 可扩展方向

| 扩展需求 | 方案 |
|----------|------|
| 持久化存储 | 替换 `InMemoryChatMessageHistory` 为数据库实现 |
| Token 限制 | 在 `get_session_history` 中添加历史截断逻辑 |
| 分布式部署 | 使用 Redis 等共享存储替代内存字典 |

这是构建 **AI 助手/客服机器人** 的基础模式，核心价值在于零侵入式地实现上下文保持。